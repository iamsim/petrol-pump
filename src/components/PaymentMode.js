import { Typography } from "@mui/material";

const PaymentMode = () => {
  return (
    <>
      <Typography variant="h6" pl={2} pt={2}>
        Payment Mode
      </Typography>
    </>
  );
};

export default PaymentMode;

import { Typography } from "@mui/material";

const Expense = () => {
  return (
    <>
      <Typography variant="h6" pl={2} pt={2}>
        Expense
      </Typography>
    </>
  );
};

export default Expense;
